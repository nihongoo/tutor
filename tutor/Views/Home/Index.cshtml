@{
	ViewData["Title"] = "Home Page";
	var a = ViewData["acc"];
	var b = TempData["login"];
	var c = ViewData["kq"];
	var d = Context.Request.HttpContext.Session.GetString("laiKep");
}

<div class="text-center">
	<h1 class="display-4">Welcome</h1>
	@if (a != null)
	{
		<div class="alert alert-danger">
			@b
		</div>

	}
	else
	{
		<a href="@Url.Action("Login","Account")" class="btn btn-success">Đăng nhập</a>
	}
	@using (Html.BeginForm("BocBatHo", "Home", FormMethod.Post))
	{
		<div class="form-group">
			<label for="m">số tháng:</label>
			<input type="text" id="m" name="m" class="form-control" />
		</div>
		<div class="form-group">
			<label for="mn">Số tiền gửi:</label>
			<input type="text" id="mn" name="mn" class="form-control" />
		</div>
		<div class="form-group">
			<label for="lai">tỉ lệ lãi:</label>
			<input type="text" id="lai" name="lai" class="form-control" />
		</div>
		<div class="d-flex flex-row-reverse mt-3">
			<button type="submit" class="btn btn-dark text-light">Tính</button>
		</div>
        @if (d != null)
        {
            <div class="alert alert-info mt-3">
                @d đ
            </div>
        }

	}
	<p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
